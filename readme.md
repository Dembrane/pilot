# Pilot

- formerly called `dembrane/pdf-pilot`
- TODO: add a description

## Features

- user can upload documents
- user can edit documents metadata
- user can chat with individual documents
- user can edit global metadata
- user can chat with all the uploaded documents
- TODO: make this section better

## Rebuilding frontend for production

> In production, the generated frontend code will be directly served from FastAPI

```
cd frontend
yarn && yarn build-and-copy
```

## Run in production

- [Build the frontend](#Building-frontend-for-production) (if any changes were made to the frontend code, and commit the `server/frontend_dist` folder)
- Build [`server/Dockerfile`](/server/Dockerfile) and run

## Run locally (Development)

**Recommended option - Use devcontainer.json**

- Have docker installed (https://docs.docker.com/engine/install/)
- In VSCode, open the command palette(CMD+Shift+P) and "Reopen in Container". It takes 10-15 mins the first time around.
- Add your env keys to `server/.env`
  ```
  cd server
  cp .env.sample .env
  ```
- Run migrations (see [Database migrations](#Database-migrations))
- Run the server

  ```
  cd server
  pip install -r requirements.txt
  ./run.sh
  ```

- Run the frontend

  > In development, Vite(frontend development server) proxies the FastAPI server running on `:8000` to `/api` (see [`/frontend/vite.config.ts`](/frontend/vite.config.ts))

  ```
  cd frontend
  yarn
  yarn dev
  ```

## Database migrations

1. Generate migrations (run this when you make changes to the models)

```
cd server
alembic revision --autogenerate -m "message"
```

2. Apply migrations (run this when you want to apply the changes to the database)

```
cd server
alembic upgrade head
```

## API reference

OpenAPI docs are auto-generated by FastAPI.

- [http://localhost:8000/docs](http://localhost:8000/docs)
- [https://pilot.findcommonground.online/docs](https://pilot.findcommonground.online/docs)

## TODOs

- [ ] use postgres instead of sqlite. consider using pgvector extension
- [ ] add db migrations
- [ ] make task queue more reliable
