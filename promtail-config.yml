server:
  http_listen_address: 0.0.0.0
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # - job_name: journal
  #   journal:
  #     max_age: 12h
  #     labels:
  #       job: "journal"
  #     path: /var/log/journal
  #   relabel_configs:
  #     # Match entries for Docker containers only
  #     - source_labels: [__journal__systemd_unit]
  #       regex: "docker.service"
  #       action: keep
  #     # Capture container names from the journal logs
  #     - source_labels: [__journal__container_name]
  #       target_label: container_name
  #     # Optionally, add other container-related labels
  #     - source_labels: [__journal__container_tag]
  #       target_label: container_tag
  #     # Use the unit label to distinguish logs
  #     - source_labels: [__journal__systemd_unit]
  #       target_label: unit

  # - job_name: containers
  #   static_configs:
  #     - targets:
  #         - localhost
  #       labels:
  #         job: containerlogs
  #         __path__: /var/lib/docker/containers/*/*log

  #   pipeline_stages:
  #     - json:
  #         expressions:
  #           log: log
  #           stream: stream
  #           time: time
  #           tag: attrs.tag
  #           compose_project: attrs."com.docker.compose.project"
  #           compose_service: attrs."com.docker.compose.service"
  #           stack_name: attrs."com.docker.stack.namespace"
  #           swarm_service_name: attrs."com.docker.swarm.service.name"
  #           swarm_task_name: attrs."com.docker.swarm.task.name"
  #     - regex:
  #         expression: "^/var/lib/docker/containers/(?P<container_id>.{12}).+/.+-json.log$"
  #         source: filename
  #     - timestamp:
  #         format: RFC3339Nano
  #         source: time
  #     - labels:
  #         stream:
  #         container_id:
  #         tag:
  #         compose_project:
  #         compose_service:
  #         stack_name:
  #         swarm_service_name:
  #         swarm_task_name:
  #     - output:
  #         source: log

  - job_name: containers

    static_configs:
      - targets:
          - localhost
        labels:
          job: containerlogs
          __path__: /var/lib/docker/containers/*/*log

    # --log-opt tag="{{.ImageName}}|{{.Name}}|{{.ImageFullID}}|{{.FullID}}"
    pipeline_stages:
      - json:
          expressions:
            stream: stream
            attrs: attrs
            tag: attrs.tag

      - regex:
          expression: (?P<image_name>(?:[^|]*[^|])).(?P<container_name>(?:[^|]*[^|])).(?P<image_id>(?:[^|]*[^|])).(?P<container_id>(?:[^|]*[^|]))
          source: "tag"

      - labels:
          tag:
          stream:
          image_name:
          container_name:
          image_id:
          container_id:
