version: '3'

services:
  api-server:
    build: ./server
    restart: always
    command: /bin/sh /code/server/prod.sh
    env_file:
      - server/.env
    ports:
      - "8000:8000"
    volumes:
      - ./server:/code/server
  