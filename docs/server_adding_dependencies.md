# Server: Adding Dependencies

We use `rye` to manage our dependencies. It uses `uv` under the hood.

[Basics of rye](https://rye.astral.sh/guide/basics/)

example: `rye add ffmpeg-python` # Add a dependency
